using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class SkillUnlockManager : MonoBehaviour
{
    //[SerializeField] private SkillData[] skillDatas;

    // Start is called before the first frame update
    void Start()
    {
        //if (SceneManager.GetActiveScene().name == "NewGameplay" || SceneManager.GetActiveScene().name == "TestingScene")
        //{
        //    Debug.Log("Unlock skill 1");
        //    for (int i = 0; i < skillDatas.Length; i++)
        //    {
        //        if (skillDatas[i].skillType == 1)
        //        {
        //            Debug.Log("Skill 1 " + skillDatas.Length);
        //            skillDatas[i].isUpgrade = true;
        //        }
        //    }
        //}
        //else if (SceneManager.GetActiveScene().name == "NewGameplay 1")
        //{
        //    Debug.Log("Unlock skill 2");
        //    for (int i = 0; i < skillDatas.Length; i++)
        //    {
                
        //        if (skillDatas[i].skillType == 2)
        //        {
        //            Debug.Log("Skill 2 " + skillDatas.Length);
        //            skillDatas[i - 1].isUpgrade = true;
        //            skillDatas[i].isUpgrade = true;
        //            skillDatas[i].skillLevel++;
        //        }
        //    }
        //}
        //else if (SceneManager.GetActiveScene().name == "NewGameplay 2")
        //{
        //    Debug.Log("Unlock skill 3");
        //    for (int i = 0; i < skillDatas.Length; i++)
        //    {
                
        //        if (skillDatas[i].skillType == 2)
        //        {
        //            Debug.Log("Skill 3 " + skillDatas.Length);
        //            skillDatas[i - 2].isUpgrade = true;
        //            skillDatas[i - 1].isUpgrade = true;
        //            skillDatas[i - 1].skillLevel++;
        //            skillDatas[i].isUpgrade = true;
        //            skillDatas[i].skillLevel++;
        //        }
        //    }
        //}
    }
}
